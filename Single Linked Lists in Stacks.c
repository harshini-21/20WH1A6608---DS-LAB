#include <stdio.h>
#include <stdlib.h>
struct node{
int data;
struct node * link;
struct node * current;
struct node * temp;
struct node * top  = NULL;
};
void push(int element)
    current = (struct node *)malloc(size of (struct node));
    current -> link = NULL;
if(top == NULL)
    current -> link = NULL;
else{
    current -> link = top;
    top = current;
}
case - 1:
    printf("Enter element");
    printf("%d", element)
    push(element);
    break;
case - 2:
    printf("deleted element is %d");
    pop();
    break;
    }
case - 3:
    if(top == NULL)
    printf("Stack Underflow");
    else
    printf("top most element is %d");
    peek();
    break;
case - 4:
    if(top == NULL)
    printf("Stack Underflow");
    else
        display();
        break;
case - 5:
    exit(0);
    }
}
}
